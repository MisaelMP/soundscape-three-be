{
	"version": 2,
	"builds": [
		{
			"src": "index.ts",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/socket.io/(.*)",
			"dest": "index.ts",
			"headers": {
				"Connection": "Upgrade",
				"Upgrade": "websocket"
			}
		},
		{
			"src": "/(.*)",
			"dest": "index.ts"
		}
	]
}
